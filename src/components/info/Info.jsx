import React from 'react';
import './info.css';

import Button from '../../components/button/Button';

const Info = props => {
  return (
    <div className="info icon-bread">
      <Button> Toevoegen </Button>
    </div>
  );
};

export default Info;
